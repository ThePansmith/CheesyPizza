// I know the code is bad its temp

if global.panic
	image_angle += (rotspd * 2 * sign(image_xscale));

//if (!global.panic)
//	image_angle += (abs(sin(scr_current_time() / 625) * rotspd) * sign(image_xscale));
//else
//image_angle += (rotspd * 2 * sign(image_xscale));

